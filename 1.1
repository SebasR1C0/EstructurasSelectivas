#include <iostream>
using namespace std;
long long a, b, c, d, e;
int main() {
    cout << "Escribre tu numero" << endl;
    cin >> a;
    b = a % 2;
    if (b == 0) {
        cout << "El numero es par";
    }
    else cout << "El numero es impar";
    return 0;
}
